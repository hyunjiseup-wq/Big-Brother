# 실행·종료 점검 및 파일 정리 — 2026-09-24

## 수정 사항

- `stop_bot.bat --check`: 괄호 블록 안에서 미리 확장되던 `%errorlevel%` 때문에
  PowerShell의 중지 상태(1)와 조회 실패(2)가 0으로 반환되던 문제를 수정했다.
  별도 라벨에서 검사 후 실제 코드를 반환하며 한국어 바로가기에도 전달된다.
- `run_bot.bat`: 상태·환경·연결 점검 및 수동 백업이 `.automod-stop-request`를
  삭제하지 않도록 했다. 유효한 무인자 시작이 환경 점검을 통과했을 때만 이전 요청을 초기화한다.
- 잘못된 실행 옵션은 Python·DB 초기화 전에 종료 코드 6으로 거부한다.
- 임시 폴더의 가짜 Python 모듈 및 PowerShell 검사기를 사용하는 Windows BAT 실행 테스트를
  추가했다. 실제 Discord 봇, 운영 DB, 외부 API 또는 실행 중인 프로세스를 사용하지 않는다.
- 봇 안의 오래된 `dashboard-site/` 복사본을 제거했다. 대시보드의 최신 소스는
  별도 `Big-Brother-Dashboard` 저장소에 유지하며 README와 `.gitignore`에 위치를 명시했다.

## 검증 근거

수정 전 격리 실행에서 종료 코드 오반환, 진단 명령의 종료 요청 삭제, 잘못된 옵션의 DB 접근을
재현했다. 수정 후 점검에서는 반환 코드 0/1/2 전달, 종료 요청 유지, 잘못된 옵션의 DB 미접근을
확인했다. 무인자 실행은 이전 요청을 지운 뒤 정상 종료 시 한 번만 실행되는지도 검사한다.

실행 명령(봇의 전용 Python 3.13 환경):

```text
python -B -m unittest discover -s tests -q
```

전체 262건 통과, 실패·건너뛰기 0건, 종료 코드 0(78.328초).
출력의 제공자 실패·전송 실패 로그는 테스트가 의도적으로 주입한 오류 시나리오다.

별도 대시보드는 `npm test`로 새 빌드 및 7개 테스트를 통과했다. 미사용 notes 예제 2개를
제거해도 로그인 화면, 비밀번호 세션, 접근 제한, 기간 조회, 제재 원장, 외부 관리봇 수집 검증이
통과했다. 이는 실제 운영 서버에 로그인하거나 실데이터 동기화를 검증한 결과는 아니다.

## 운영 영향과 남은 확인

- 관리자 수동 검수 방식과 실제 제재 정책은 바꾸지 않았다.
- 이번 작업 중 운영 봇을 시작·중지하거나 공개 사이트를 재배포하지 않았다.
- 라이브 Discord 연결 상태의 수동 종료·재실행과 네트워크 장애 중 종료는 운영 확인이 남아 있다.
- 이전 파일 검수에서 발견한 창 이동 시작프로그램 바로가기는 그대로다. 필요 여부 확인과
  비활성화는 별도 운영 작업이며 이번 수정의 범위에 포함하지 않았다.
- AI 오탐 감소 효과, 무료 API 잔여량, 운영 사이트의 최신 수신 상태는 이번 테스트로 판단하지 않는다.

파일 정리 상세 내역은 로컬 작업영역의 `정리폴더/파일정리_2026-09-24.md`에 보관한다.
운영 데이터·백업·인증 파일은 버전 관리에 새로 추가하지 않는다.

## 후속 개선 — 종료 대상 식별과 실패 보고

기준 커밋: `7737220`. 종료기의 대상 조건이 폴더 경로와 임의 `.bat` 문자열까지 포함하고,
강제 종료 오류를 무시한 채 완료를 출력할 수 있는 부분을 보완했다.

- 전용 Python 실행 파일과 해당 프로젝트의 절대경로 `bot.py`를 함께 확인한다.
  `run_bot.bat`도 `bot.py`를 절대경로로 실행한다.
- 구형 상대경로 `bot.py`는 해당 프로젝트 실행 BAT를 호출한 부모 CMD가 확인될 때만 대상으로
  삼는다. 소속을 확인할 수 없으면 조회·종료 코드 2를 반환한다.
- 재시작 러너는 `run_bot.bat` 또는 한국어 실행 바로가기를 직접 실행하는 전용 `/c` CMD만
  식별한다. 다른 BAT, 진단 인자, 다른 경로, 명령을 이어 붙인 쉘은 대상에서 제외한다.
- `/k` 대화형 CMD의 봇은 부모 확인에 활용하지만 그 CMD 창 자체는 강제로 닫지 않는다.
- 강제 종료 직전 PID의 생성 시각·실행 파일·명령줄을 다시 확인한다. PID가 재사용된 경우
  종료하지 않는다. 이후 대상을 재조회해 남아 있으면 성공으로 처리하지 않는다.
- 권한 거부·프로세스 잔존·식별 변경은 코드 2와 오류로 알리고 종료 요청을 보존한다.
  요청 파일을 쓸 수 없는 경우에는 그 실패도 명시한다.

검증: 전체 `unittest` 268건 통과, 종료 코드 0, 70.034초. 새 종료기 검증 6건은 가짜 Windows
프로세스 목록·종료 함수를 사용한다. 정상 종료, 요청 파일의 러너 소비, 다른 프로그램 제외,
레거시 상대경로, 대화형 터미널 보존, 권한 거부, 종료 후 잔존, PID 재사용, 조회 실패를 포함한다.
실제 프로세스를 종료하는 테스트로 오해하지 않아야 한다.

실제 PC의 읽기 전용 `stop_bot.ps1 -Check` 조회는 `[STOPPED]`와 코드 1을 반환했다.
현재 실행 중인 BB봇은 확인되지 않았다. 라이브 Discord 연결을 통한 정상 종료 및 재실행 검증은
여전히 남아 있으며, 시작프로그램·제재 방식·사이트 배포 설정은 변경하지 않았다.

## 후속 개선 — 자동 시작 해제 및 전역 창 이동기 제거

기준 커밋: `9d7a604`. 앞선 작업에서 보류했던 시작프로그램을 실제 PC 권한으로 다시 조회했다.
`BB Console Window Mover.lnk`가 이 프로젝트의 `console_window_mover.vbs`를 호출하고,
해당 PowerShell 창 이동기(PID 6236)가 실행 중임을 확인했다. 이 도구는 봇 창만이 아니라
모든 보이는 콘솔 창을 50ms 주기로 확인하여 2번 모니터로 이동시키던 구형 코드였다.

- 대상·인수·작업 폴더가 일치하는 해당 시작프로그램 바로가지만 삭제했다.
- 실행 파일·명령줄·생성 시각을 재확인한 창 이동기만 종료했다. 다른 터미널은 종료하지 않았다.
- 더 이상 참조되지 않는 로컬 `console_window_mover.ps1` / `.vbs`를 삭제했다.
  이 두 파일과 바로가기는 휴지통 보관 없이 삭제했으며 Git 미추적 파일이라 Git 복원은 불가능하다.
  운영 데이터·백업·인증 파일은 삭제하지 않았다.
- `register_startup.bat`의 알 수 없는 옵션이 자동 시작 등록으로 이어지던 문제를 수정했다.
- 바로가기 처리를 `startup_shortcut.ps1`로 분리하고 `--remove`, `시작프로그램_해제.bat`을 추가했다.
  등록·해제 전 실행 대상·작업 폴더·인수를 확인하여 다른 설치본의 바로가기를 보존한다.
- 숨김 실행 파일이 없어도 등록 상태 조회와 해제를 할 수 있으며, 등록만 실패 처리한다.
- 등록·조회·해제의 실제 종료 코드를 전달하고 무인 실행 시 키 입력을 기다리지 않는다.

검증: Windows 실제 COM 바로가기를 임시 APPDATA에 생성하는 통합 테스트 6건 추가.
등록/조회/반복 해제, 잘못된 옵션, 타 설치본 보존, 실행 파일 누락, 창 표시 설정 복구,
바로가기 쓰기 실패를 검증했다. 테스트 중 실제 Startup에는 등록하지 않았고 봇을 시작하지 않았다.
전체 `unittest` **274건 통과**, 실패·건너뛰기 0건, 종료 코드 0(89.552초).

실제 PC 후속 조회 결과:

- 봇 자동 시작 조회: `[NOT REGISTERED]`, 코드 2.
- 봇 프로세스 조회: `[STOPPED]`, 코드 1.
- 구형 창 이동기 바로가기 없음, 실행 중인 창 이동기 0개.
- 프로젝트 경로·관련 이름을 기준으로 조회한 예약 작업 0개.

수동 BAT 실행 운영을 유지한다. 새 터미널을 반드시 2번 모니터에서 생성하는 기능은 없으며,
이번 정리가 모든 프로그램에서 발생하는 창 팝업 문제를 해결했다고 단정하지 않는다.
다음 Windows 로그인 후 재발 여부와 실제 Discord 연결 중 종료·재실행 확인은 남아 있다.
제재 정책·AI 판단 로직·대시보드는 변경하거나 재배포하지 않았다.

## 후속 개선 — AI 장애 재검사 작업의 복구성

기준 커밋: `a0aa0af`. 재검사 대기열을 조회하거나 재시도 일정을 저장하는 일부 경로의 예외가
백그라운드 작업 밖으로 전파되고, 워커는 최초 연결 때만 생성되므로 봇 프로세스가 살아 있어도
재검사만 멈출 수 있었다. 또한 `get_guild()`가 None이면 보류 기록을 즉시 삭제하고 있었다.

- 재검사 루프를 감독하는 함수를 추가하여 일반 오류 발생 시 기본 5초 대기 후 재개한다.
- 취소 요청은 그대로 전달하여 수동 종료 중 재시작하지 않는다.
- 대기열 조회 전과 각 메시지 처리 전에 Discord 연결 준비 상태를 기다린다.
- 서버 정보가 없거나 일시 사용 불가 상태이면 삭제 대신 지연 재시도한다.
- 오류 출력에는 예외 유형만 남긴다. API 한도나 AI 판단 기준, 관리자 수동 검수 정책은 그대로다.

새 회귀 테스트로 수정 전 DB 예외 전파, 서버 정보 누락 시 삭제, 연결 준비 확인 누락을 재현했다.
수정 후 신규 7건과 기존 재검사 테스트가 통과했다. DB 조회 실패 후 재개와 실제 다음 항목 처리,
일정 저장 실패 복구, 서버 정보 없음/사용 불가 시 보존, 연결 준비 대기, 항목 사이 재연결 확인,
오류 대기 중 수동 종료를 포함한다. Discord·DB 호출은 테스트 대역이며 운영 서비스는 호출하지 않는다.

전체 검증: `python -B -m unittest discover -s tests -q`, **281건 통과**, 실패·건너뛰기 0건,
종료 코드 0(55.136초). `git diff --check` 통과. README에 동작·운영 한계를 함께 반영했다.

남은 내용:

- 실제 Discord 연결 단절/복구 및 DB 일시 장애에서의 운영 검증.
- 실제 탈퇴 서버의 잔존 큐를 안전하게 식별·정리하는 운영 절차. 현재는 보존을 우선한다.
- 재검사 결과를 검수 기록으로 넘기는 구간의 내구성 보완. 기존 코드에 큐 삭제가
  `handle_violation()`보다 먼저 수행되는 경로가 남아 있어, 중간 실패 시 누락 위험이 있다.
  삭제 순서만 바꾸면 이미 처리된 검수/조치를 중복 실행할 수 있으므로 완료 기록과 중복 방지를
  함께 설계해야 한다. 이번 커밋의 복구 보장은 큐 조회·연결 준비·서버 조회·재시도 저장 경로에 한정한다.
- 장기 오탐률 개선 효과는 이번 회귀 테스트로 확인하지 않았다.

운영 봇을 시작·재시작하지 않았고 자동 시작·대시보드 배포·운영 데이터는 변경하지 않았다.

## 후속 완료 — 재검사 인계 내구성·실연결 복구·정리 기준

위 후속 과제의 검수 저장/큐 삭제 선행 문제를 원자적 트랜잭션과 재검사 번호 기반 중복 방지로
보완했다. 실제 Discord 진단 세션의 단절→RESUMED 및 복구 후 REST 조회도 확인했다.
장기 큐는 읽기 전용 조사 후보 도구와 관리자 승인 기반 정리 기준을 추가했다.
최종 전체 295건 회귀 테스트를 통과했다. 상세 증거와 보장 한계, 운영 절차는
[재검사 운영 문서](RETRY_QUEUE_OPERATIONS.md)에 기록했다.
